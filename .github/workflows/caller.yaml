name: Caller Workflow

on:
  push:

jobs:
  call-workflow-1-in-local-repo:
    uses: lucetre/action-reusable-workflow/.github/workflows/callee.yaml@12083a0c7b7ce8f598efaabca9a93fc15c789327
    with:
      username: outsider
    secrets:
      SECRET_SEED: world
  
  call-workflow-2-in-local-repo:
    uses: ./.github/workflows/callee.yaml
    with:
      username: mona
    secrets:
      SECRET_SEED: seed
  
  post-call:
    runs-on: ubunut-latest
    steps:
      - run: echo "DONE"
